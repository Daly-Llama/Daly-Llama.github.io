
/* ============================================================
   LONG-FORM POSTS
   ============================================================ */

.post-content { max-width: 42rem; }                 /* ~700px reading measure */
.post-content > * + * { margin-top: 1.25em; }
.post-content h2 { margin-top: 2.5em; }
.post-content h3 { margin-top: 2em; }

.post-content blockquote {
  border-left: 3px solid var(--accent);
  padding-left: 1.25rem;
  margin-left: 0;
  color: var(--text-secondary);
  font-style: italic;
}

.post-meta {
  color: var(--text-secondary);
  font-size: 0.9375rem;
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  align-items: center;
}

.post-footer {
  max-width: 42rem;
  margin-top: 3rem;
  padding-top: 1.5rem;
  border-top: 1px solid var(--border);
  font-size: 0.9375rem;
}

/* ---- Figures and embeds ---- */

.post-figure { margin: 2.5rem 0; }
.post-figure figcaption {
  font-size: 0.9375rem;
  color: var(--text-secondary);
  margin-top: 0.75rem;
}

.video-embed { aspect-ratio: 16 / 9; margin: 2.5rem 0; }
.video-embed iframe {
  width: 100%;
  height: 100%;
  border: 0;
  border-radius: var(--card-radius);
}

/* ============================================================
   WRITING INDEX — tags and filtering
   ============================================================ */

.post-tag {
  display: inline-block;
  background: var(--bg-tag);
  color: var(--text-secondary);
  border-radius: 999px;
  padding: 0.15rem 0.65rem;
  font-size: 0.8125rem;
  text-transform: lowercase;
}

.article-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 0.4rem;
  margin-top: 1rem;
}

.tag-filter {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin: 2rem 0;
}

.tag-chip {
  font-family: var(--font-body);
  font-size: 0.875rem;
  text-transform: lowercase;
  background: transparent;
  color: var(--text-secondary);
  border: 1px solid var(--border);
  border-radius: 999px;
  padding: 0.35rem 0.9rem;
  cursor: pointer;
  transition: background 0.15s ease, color 0.15s ease, border-color 0.15s ease;
}

.tag-chip:hover { border-color: var(--accent); color: var(--accent); }

.tag-chip.is-active {
  background: var(--accent);
  border-color: var(--accent);
  color: #fff;
}

/* Article cards are <article>, not <a> — this restores the
   whole-card click target via the title link. */
.article-card { position: relative; }
.article-card h3 a { text-decoration: none; color: inherit; }
.article-card h3 a::after { content: ""; position: absolute; inset: 0; }
.article-card:hover h3 a { color: var(--accent); }

.elsewhere-heading {
  margin-top: 4rem;
  padding-top: 2.5rem;
  border-top: 1px solid var(--border);
}

.section-lede { color: var(--text-secondary); }
.no-results { color: var(--text-secondary); font-style: italic; }
.feed-link { margin-top: 3rem; font-size: 0.9375rem; }

/* ---- Footer disclaimer ---- */
.footer-disclaimer {
  font-size: 0.8125rem;
  color: var(--text-footer);
  opacity: 0.75;
  margin-top: 0.75rem;
}
